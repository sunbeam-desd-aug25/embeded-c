
#include"header.h"

int main()
{
    int res = sub( );
    display(res);

    /*
    int num1;
    num1 = accept();// Function call to accept()
                    //but this will give error if accept() is static
                    //because static function is accessible only inside that file fun.c
    display(num1);
    */

    return 0;
}
